<?xml version="1.0" encoding="utf-8"?>
<!-- SEC-0066: exclui todos os dados do app (inclui wanotif.db, que armazena
     conteúdo de mensagens do WhatsApp) do cloud backup (Android 12+) e da
     transferência device-to-device. Redundante com allowBackup="false", mas
     mantido como defesa em profundidade caso allowBackup volte a ser habilitado. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
